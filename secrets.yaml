secrets:
  api_key: "telegram_bot_key"
  chat_ids: ["CHAT1", "CHAT2", "CHAT3"]  #Массив
  
  import yaml

with open("secrets.yaml", "r") as stream:
    try:
        secrets = yaml.safe_load(stream)
    except yaml.YAMLError as exc:
        print(exc)

print(secrets['secrets']['chat_ids'])  #обращение к файлу
